import React, {memo} from 'react';

import {SafeAreaView, Text} from 'react-native';

const AddTasks = () => {
  return (
    <SafeAreaView>
      <Text>AddTasks</Text>
    </SafeAreaView>
  );
};

export default memo(AddTasks);
