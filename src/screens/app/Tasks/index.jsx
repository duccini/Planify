import React, {memo} from 'react';

import {SafeAreaView, Text} from 'react-native';

const Tasks = () => {
  return (
    <SafeAreaView>
      <Text>Tasks</Text>
    </SafeAreaView>
  );
};

export default memo(Tasks);
